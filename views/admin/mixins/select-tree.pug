mixin select-tree(records, prefix = "", parent_category = '')
    each item in records
        option(value=item.id selected=(item.id === parent_category ? true : false)) #{prefix + item.title}
        if item.children && item.children.length
            +select-tree(item.children, prefix + "-- ", parent_category)
